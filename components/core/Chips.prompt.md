One-line: token for a selected value or applied filter; add `onDelete` when it can be removed.

```jsx
<Chips label="Site inspection" onDelete={remove} />
<Chips label="Overdue" color="var(--negative-color-selected)" size="small" />
```
