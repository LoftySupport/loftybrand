One-line: pick a value in a bounded range where the exact number matters less than the relative position.

```jsx
<Slider label="Radius (km)" value={r} onChange={setR} min={1} max={50} showValue />
```
