One-line: glyph-only action for toolbars, table rows and card corners; pair with Tooltip.

```jsx
<IconButton icon="MoreActions" ariaLabel="More actions" />
<IconButton icon="Add" kind="primary" size="small" />
```
