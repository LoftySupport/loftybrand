One-line: in-page notice that stays put — use Toast for transient confirmations.

```jsx
<AttentionBox type="warning" title="Two jobs unassigned" text="Assign a crew before publishing the schedule." />
```
