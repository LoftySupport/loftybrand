One-line: hides detail that most users do not need — advanced settings, a job's full history.

```jsx
<ExpandCollapse title="Advanced scheduling" subtitle="Buffers, dependencies, blackout dates">
  <SchedulingFields />
</ExpandCollapse>
```

Never hide something required to complete the task. For several of these in a row, use Accordion so only one opens at a time.
