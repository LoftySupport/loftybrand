One-line: use for every glyph — never hand-roll SVG or use emoji.

```jsx
<Icon name="Search" size={18} />
<Icon name="Check" color="var(--positive-color)" />
```

Names are the file stems in `assets/icons`. Set `basePath` when the page is not two levels below the project root.
