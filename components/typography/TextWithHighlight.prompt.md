One-line: search-result text with the query highlighted.

```jsx
<TextWithHighlight text="Northline resurfacing — Stage 2" highlightTerm="north" />
<TextWithHighlight text={row.notes} highlightTerm={q} useEllipsis maxLines={2} />
```

Highlight fill is the Selected tint so it reads as "matched", not "warning". Pair with Search or Combobox.
