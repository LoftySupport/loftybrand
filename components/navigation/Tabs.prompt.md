One-line: switches between views of the same object — not app-level navigation.

```jsx
<Tabs tabs={[{id:"table",label:"Table",icon:"Table"},{id:"map",label:"Map",icon:"Location"}]} value={v} onChange={setV} />
```
