One-line: the container primitive — a div that speaks tokens.

```jsx
<Box padding="medium" border rounded="medium" shadow="small">…</Box>
<Box backgroundColor="grey" paddingX="large" paddingY="small">…</Box>
```

Use instead of hand-writing padding/border/radius on wrappers so surfaces stay on the token scale.
